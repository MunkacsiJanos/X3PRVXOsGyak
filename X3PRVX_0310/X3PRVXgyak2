#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

int main()
{
    printf("Exit: Ctrl + C\n");
    while(true)
   	 {
     	   char cmd[50];
     	   printf("Type in a UNIX command!\n");
     	   fgets(cmd, 50, stdin);
      	  system(cmd);
  	  }
    
    return 0;
}

